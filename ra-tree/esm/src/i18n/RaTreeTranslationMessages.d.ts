export default interface RaTreeTranslationMessages {
    'ra-tree': {
        drag_preview: string;
        root_target: string;
        item_moved: string;
        new_node: string;
        action: {
            add_root: string;
            add_child: string;
        };
    };
}
