import React from 'react';
declare const renderSwitcherIcon: (className: string, switcherIcon: React.ReactNode | null | undefined, showLine: boolean | undefined, { isLeaf, expanded, loading, }: {
    isLeaf?: boolean;
    expanded?: boolean;
    loading?: boolean;
}) => any;
export default renderSwitcherIcon;
