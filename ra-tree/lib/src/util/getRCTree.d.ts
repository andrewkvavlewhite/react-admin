import { DataNode } from 'rc-tree/lib/interface';
import { TreeRecord } from '../types';
export declare const getRCTree: (data: TreeRecord[], titleField: string, newNodeTitle?: string, hideRootNodes?: boolean) => DataNode[];
